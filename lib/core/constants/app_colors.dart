import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00E5FF);       // Cyan accent
  static const secondary = Color(0xFF76FF03);     // Green for completion
  static const background = Color(0xFF0A0A0F);    // Near black
  static const surface = Color(0xFF13131A);       // Card background
  static const surfaceVariant = Color(0xFF1E1E2A); // Slightly lighter
  static const onSurface = Color(0xFFE8E8F0);
  static const onSurfaceMuted = Color(0xFF8888AA);
  static const error = Color(0xFFFF5252);

  static const easyRun = Color(0xFF4CAF50);
  static const tempoRun = Color(0xFFFF9800);
  static const intervalRun = Color(0xFFF44336);
  static const longRun = Color(0xFF9C27B0);
  static const crossTrain = Color(0xFF2196F3);
  static const rest = Color(0xFF607D8B);
}
