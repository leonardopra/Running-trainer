{
  "name": "running-trainer-web-app",
  "private": true,
  "version": "0.1.0",
  "description": "Native web client for Running Trainer.",
  "scripts": {
    "dev": "echo \"Scaffold only: choose bundler and add app entrypoint next\"",
    "test": "echo \"Add parity and UI tests\""
  }
}
